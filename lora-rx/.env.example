# ==============================
#  Configuración .env.example (LoRa RX)
# ==============================
# Copia este archivo a ".env" y ajusta los valores según tu hardware y región.
# Recomendación: FREQ y AIRSPEED deben coincidir EXACTAMENTE con el TX.

# --- Hardware / Puerto serie ---
# /dev/ttyUSB0  → si usas adaptador USB-serial (jumper en A)
# /dev/serial0  → si usas GPIO (jumper en B)
SERIAL=/dev/ttyUSB0
#SERIAL=/dev/serial0

# --- Radio ---
# Frecuencia en MHz (868 o 915 según tu módulo y región)
FREQ=915

# Dirección propia del RX (diferente a la del TX). Ej.: si TX=101, RX=102
ADDR=0

# Potencia de radio (parámetro del driver; mantener valor razonable)
POWER=22

# Velocidad de aire (bps) — debe coincidir en TX y RX
AIRSPEED=2400

# --- Registro / Depuración ---
# Ruta del CSV para guardar tramas recibidas. Vacío para desactivar.
# Si es ruta relativa, se crea respecto al directorio del proyecto lora-rx.
RX_CSV=./rx_log.csv

# Depuración (0 = off, 1 = on) para ver datos brutos del puerto serie
RX_DEBUG=0

# --- Notas ---
# - Si el TX usa DEST=65535 (broadcast), este RX recibirá si FREQ/AIRSPEED coinciden.
# - Para direccionamiento específico, en el TX usa DEST=<ADDR de este RX>.
# - Ejecutar RX:   bash scripts/run_rx.sh
# - Ejecutar TX:   bash ../lora-tx/scripts/run_tx.sh
