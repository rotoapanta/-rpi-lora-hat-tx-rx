# Archivo de ejemplo .env — copia como .env y ajusta según tu entorno

# Puerto serie del SX126x
#   /dev/ttyUSB0  → si usas adaptador USB-serial (jumper en A)
#   /dev/serial0  → si usas GPIO (jumper en B)
SERIAL=/dev/ttyUSB0

# Radio
# Frecuencia en MHz (868 o 915 según tu módulo y región)
FREQ=915

# Dirección propia del TX
ADDR=101

# Dirección de destino
#   65535 = broadcast
#   o la dirección del RX (ej. 102)
DEST=65535

# Potencia de transmisión en dBm (máx. según módulo)
POWER=22

# Velocidad de aire (bps) — debe coincidir en TX y RX
AIRSPEED=2400

# Transmitters
# Periodo de envío en segundos
PERIOD=1.0

# Tipo de transmisor:
#   random   → datos aleatorios
#   sensors  → simulación de lluvia + sísmico
TX_TYPE=sensors

# Solo para TX_TYPE=random
MODE=json

# Solo para TX_TYPE=sensors
STATION=REVN       # Identificador de la estación
BUCKET_MM=0.2      # mm por baldeo (tipping bucket)
